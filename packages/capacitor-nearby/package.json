{
  "name": "@tarot/capacitor-nearby",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin Capacitor bridge over Google Nearby Connections (P2P_STAR)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "files": ["dist", "android"],
  "capacitor": {
    "android": {
      "src": "android"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/core": "^8.5.2"
  }
}
